# URL del backend (API) de M6.
# En desarrollo local apunta al backend corriendo en localhost.
# En producción se reemplaza por la URL del backend desplegado.
NEXT_PUBLIC_API_URL=http://localhost:3000
